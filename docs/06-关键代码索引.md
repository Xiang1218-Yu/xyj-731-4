# 06 · 关键代码索引

> 所有路径相对于本目录；行号基于当前克隆的 master 分支源码。

## prosemirror-model

| 主题 | 文件 | 关键位置 |
|---|---|---|
| Schema 编译与约束 | `prosemirror-model/src/schema.ts` | `Schema.constructor` L596；`NodeType` L60；`createChecked` L160；`validContent` L188；`MarkType` L281；`excluded` 求值 L622-625 |
| 内容表达式自动机 | `prosemirror-model/src/content.ts` | `ContentMatch` L10；`matchFragment` L43；`fillBefore` L79 |
| 不可变文档节点 | `prosemirror-model/src/node.ts` | `Node` L22（持久化设计注释 L14-18）；`nodeSize` L54；`copy` L138；`slice/replace` L159-177；`TextNode` L353 |
| 子节点容器 | `prosemirror-model/src/fragment.ts` | `Fragment` L10；`replaceChild` L115；`findIndex` L193；`fromArray` 文本合并 L227 |
| Mark 集合代数 | `prosemirror-model/src/mark.ts` | `Mark` L10；`addToSet` L24；`removeFromSet` L49；`setFrom` L101 |
| 位置解析 | `prosemirror-model/src/resolvedpos.ts` | `ResolvedPos` L12；path 三元组结构 L19-28；`nodeBefore/nodeAfter` L100-115 |

## prosemirror-transform

| 主题 | 文件 | 关键位置 |
|---|---|---|
| 原子步骤抽象 | `prosemirror-transform/src/step.ts` | `Step` L16（apply/invert/map/getMap 契约）；`StepResult` L71 |
| Replace 步骤 | `prosemirror-transform/src/replace_step.ts` | `ReplaceStep.apply` L28；`getMap` L34；`invert` L38；`merge` L49 |
| 变更批次 | `prosemirror-transform/src/transform.ts` | `Transform` L28（steps/docs/mapping）；`addStep` L89；`replace` L98 |
| 位置映射 | `prosemirror-transform/src/map.ts` | `StepMap` L72；`_map` L98；`MapResult` L40；`Mapping.map` L252；mirror/rebase L225-249 |

## prosemirror-state

| 主题 | 文件 | 关键位置 |
|---|---|---|
| 状态机 | `prosemirror-state/src/state.ts` | `baseFields` L21；`applyTransaction` L137（filter/append 管线）；`applyInner` L171；`reconfigure` L199 |
| 事务 | `prosemirror-state/src/transaction.ts` | `Transaction extends Transform` L42；惰性选区 L71-77；`setMeta` L187；`insertText` L165 |
| 选区 | `prosemirror-state/src/selection.ts` | `Selection` L9；`TextSelection` L229；`NodeSelection` L325；`AllSelection` L399；`near` L135；`findSelectionIn` L439；`selectionToInsertionEnd` L454 |
| 插件系统 | `prosemirror-state/src/plugin.ts` | `PluginSpec` L7；`StateField` L95；`Plugin` L71；`PluginKey` L129 |

## prosemirror-view

| 主题 | 文件 | 关键位置 |
|---|---|---|
| 编辑器外壳 | `prosemirror-view/src/index.ts` | `EditorView` L30；`updateStateInner` L153；`updatePluginViews` L255；`someProp` L294 |
| DOM 输入回收 | `prosemirror-view/src/domchange.ts` | `readDOMChange` L81（diff → DOMParser → Transaction） |
| 视图描述树 | `prosemirror-view/src/viewdesc.ts` | ViewDesc 树：文档 → DOM 的增量同步 |
| DOM 选区 | `prosemirror-view/src/selection.ts` | 模型选区 ↔ DOM Range 双向同步 |

---

**返回**：[README 索引](../README.md)
